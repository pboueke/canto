# v0.6.0 — Cleanup 1 (Requirement 6)

Commit: 9fd6d90 | Tag: v0.6.0

## What was done

### Phase 1: Security Hardening

- Added `clearKey()` to device encryption (`device.ts`) — zeros cached key with `.fill(0)` and nulls it
- Added `clearSession()` to encryption service (`index.ts`) — calls device `clearKey()`
- Added `validatePasswordStrength()` in `password.ts` — minimum 8 characters
- Created `ratelimit.ts` — `createUnlockRateLimiter()` with 5 attempts and 30s lockout
- Wired password validation into `NewJournalModal` and rate limiter into `JournalAccessModal`
- Removed unused `expo-crypto` dependency

### Phase 2: Test Suite Expansion

- New tests: device encryption, encryption service, password validation, rate limiter, model types, localStorage
- Relocated root `__tests__/` files to colocated `src/*/__tests__/` directories
- Updated jest.config.js with 40% statement coverage threshold (excluding components/app screens)
- Result: 81 tests across 9 suites, all passing

### Phase 3: Repository Hooks & Tooling

- Added `npm audit --production` script and Makefile target
- Added `npm test` to `.husky/pre-push` hook
- Added GPL-3.0 LICENSE file (downloaded from gnu.org — cannot be generated by Claude due to content filter)

### Phase 4: Repository Structure

- Added `.nvmrc` with Node 20
- Bumped version to 0.6.0
- Fixed package.json indentation issue from expo-crypto removal

### Phase 5: README Overhaul

- Complete rewrite with badges, feature list, architecture docs (two-tier encryption), project structure, contributing guide

## Notes

- The GPL-3.0 license text triggers Anthropic's content filtering policy — must be downloaded via curl, never generated
- Added `coverage/` to `.gitignore` (was missing)
