# v0.17.4 — Low priority fixes

Addresses 10 LOW priority issues from the codebase audit (`.claude/notes/2026-03-20-6.md`).

## Changes

### Phase 1: Validation Polish

- **#31** GeoLocation range checks — latitude [-90, 90], longitude [-180, 180]
- **#32** Empty/whitespace tag rejection in `validatePage()`

### Phase 2: Minor Hardening

- **#33** Random multipart boundary per GDrive API request (replaces hardcoded string)
- **#34** `errorStack` preserved in `SyncState` for debugging
- **#36** Tag normalization via `useMemo` in `TagEditor` — lowercases, trims, deduplicates
- **#39** Runtime shape validation of stored Google auth JSON
- **#40** 15s timeout on `getAccessToken` silent re-auth

### Phase 3: Storage Cleanup

- **#35** Non-blocking attachment cleanup on soft delete (native + web)

### Phase 4: Test Gaps

- **#38** InfoBox `act()` warnings fixed with `waitFor()`
- **#37** 6 new test files: downloadAttachment (native), FilterBar debounce, ImageCarousel edit flow, PageListItem rendering, TagEditor normalization, accessibility audit

## Test Results

763 tests pass across 56 suites. No regressions.
