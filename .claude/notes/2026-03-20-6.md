# TODOs — Canto Codebase Audit

Full codebase audit performed 2026-03-20 at v0.17.0. Issues ordered by priority.

## Issue Table

| #   | Priority | Category        | File(s)                                                            | Issue                                                                                                                                              | Risk                                                                                                                   |
| --- | -------- | --------------- | ------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 1   | CRITICAL | data-corruption | `src/hooks/useStorage.ts:9-27`                                     | Race condition in store initialization — concurrent `ensureInitialized()` calls can create multiple store instances                                | Multiple components mounting simultaneously may get uninitialized store, causing encryption failures or data loss      |
| 2   | CRITICAL | data-corruption | `src/lib/encryption/device.ts:45-53`                               | Race condition in device key creation — no mutex around `getOrCreateDeviceKey()`                                                                   | Concurrent calls during startup can generate multiple keys; data encrypted with discarded key becomes unrecoverable    |
| 3   | CRITICAL | data-corruption | `src/lib/sync/gdrive/store.ts:268-282`                             | `Promise.allSettled()` silently drops failed page downloads during sync                                                                            | User receives incomplete journal without notification; silent data loss                                                |
| 4   | CRITICAL | data-corruption | `src/lib/backup/import.ts:207-213`                                 | No error handling around `saveAttachment()` during import — partial failure leaves orphaned data                                                   | Import can leave journal in inconsistent state with pages referencing missing attachments                              |
| 5   | CRITICAL | data-corruption | `packages/canto-data/src/validation.ts:55-56`                      | `checkNumber()` accepts NaN and Infinity                                                                                                           | Data with `modified: NaN` or `latitude: Infinity` passes validation; corrupts on JSON round-trip (serializes as null)  |
| 6   | HIGH     | data-corruption | `src/components/home/SecuritySettingsModal.tsx:52-94`              | Device key rotation lacks transaction semantics                                                                                                    | If re-encryption fails partway, new key is active but old-key-encrypted data remains, making it permanently unreadable |
| 7   | HIGH     | data-corruption | `src/components/journal/JournalSettings.tsx:202-213`               | `reencryptJournal` has no try-catch for partial failures                                                                                           | Re-encryption failing mid-page leaves journal with mixed encryption keys; no recovery path                             |
| 8   | HIGH     | data-corruption | `src/lib/backup/import.ts:161-175`                                 | Malformed ZIP attachments skipped silently (no user notification)                                                                                  | Corrupted backups silently lose attachments; pages reference non-existent attachment IDs                               |
| 9   | HIGH     | bug             | `src/components/home/NewJournalModal.tsx`                          | `existingIds` prop never wired by parent — cloud import "already local" detection is broken                                                        | All cloud journals shown as importable regardless of local copies; regression from v0.17.0                             |
| 10  | HIGH     | security        | `src/lib/encryption/device.web.ts:24-40`                           | Device encryption key stored in unencrypted localStorage                                                                                           | XSS vulnerability can steal the device key and decrypt all journal data; no Web Crypto key wrapping                    |
| 11  | HIGH     | bug             | `src/contexts/JournalKeyContext.tsx:102-111`                       | Auto-lock interval accumulates — missing cleanup on `clearAll` dependency change                                                                   | Multiple concurrent intervals waste CPU and trigger redundant key erasures                                             |
| 12  | HIGH     | data-corruption | `src/lib/sync/engine.ts:40-65`                                     | `downloadPageAttachments()` silently skips null attachments without updating paths                                                                 | Pages left with stale remote paths referencing non-existent local attachments                                          |
| 13  | HIGH     | data-corruption | `packages/canto-data/src/format.ts:169`                            | `deserializePages()` casts `JSON.parse()` directly to Page without validation                                                                      | Malformed JSON becomes Page objects with undefined properties; silent corruption on import                             |
| 14  | HIGH     | bug             | `src/contexts/SyncManagerContext.tsx:102-112`                      | Untracked promise in `scheduleSyncDebounced` — callback runs on stale manager after unmount                                                        | Silent failures or memory leaks if component unmounts during OAuth flow                                                |
| 15  | MEDIUM   | data-corruption | `src/components/home/NewJournalModal.tsx:192-206`                  | Cloud import `Promise.all` for attachments — one failure aborts all but metadata may be partially saved                                            | Journal imported with missing attachments; user sees empty image placeholders                                          |
| 16  | MEDIUM   | bug             | `src/lib/sync/gdrive/store.ts:114,136`                             | Inconsistent folder cache key format — collision risk for same-name folders in different parents                                                   | Attachments uploaded to wrong GDrive folder                                                                            |
| 17  | MEDIUM   | code-quality    | `src/lib/storage/local.ts` + `local.web.ts`                        | ~450 lines of duplicated business logic between native and web storage implementations                                                             | Encryption/storage bugs must be fixed in two places; high maintenance burden                                           |
| 18  | MEDIUM   | code-quality    | `src/lib/backup/export.ts` + `export.web.ts`                       | 95% identical export logic duplicated across platforms                                                                                             | Bug fixes must be applied to both files                                                                                |
| 19  | MEDIUM   | code-quality    | `src/lib/backup/import.ts` + `import.web.ts`                       | Identical import logic duplicated across platforms                                                                                                 | Same as above                                                                                                          |
| 20  | MEDIUM   | bug             | `src/lib/backup/import.ts:194-216`                                 | Attachment encryption flag set based on current key, not original intent                                                                           | Encrypted attachment can be imported as unencrypted if key not provided; no warning                                    |
| 21  | MEDIUM   | security        | `src/lib/sync/gdrive/store.ts:46-48`                               | No validation that access token is non-empty before storing                                                                                        | Silent sync failures from invalid credentials                                                                          |
| 22  | MEDIUM   | code-quality    | `src/lib/storage/local.ts`, `local.web.ts`, `sync/gdrive/store.ts` | Inconsistent `JSON.parse()` error handling — `safeJsonParse()` exists but not used everywhere                                                      | Malformed data causes uncaught exceptions, crashing sync/import                                                        |
| 23  | MEDIUM   | code-quality    | `src/lib/backup/import.ts:111-141`                                 | Type assertion `as JournalContent` without runtime validation                                                                                      | False type safety; crafted ZIP can inject wrong schema                                                                 |
| 24  | MEDIUM   | bug             | `app/page/[id].tsx:387`                                            | Unused `t` in `handleDownloadImage` dependency array                                                                                               | Unnecessary re-creation of callback on language change                                                                 |
| 25  | MEDIUM   | bug             | `src/components/journal/FilterBar.tsx:46-48`                       | Debounce sync useEffect is one-directional (clear only)                                                                                            | External query changes leave text input showing stale text                                                             |
| 26  | MEDIUM   | code-quality    | `src/hooks/useFilter.ts:18-20`                                     | Dead fallback branch — `searchText` is required, ternary always takes truthy path                                                                  | Misleading code suggesting optional field                                                                              |
| 27  | MEDIUM   | code-quality    | `app/page/[id].tsx:345-374`                                        | DRY violation — `handleOpenFile` duplicates `downloadAttachment` logic inline                                                                      | Bug fixes must be applied to both code paths                                                                           |
| 28  | MEDIUM   | bug             | `app/page/[id].tsx:209-238, 240-289`                               | `handleAddImage`/`handleAddFile` don't try-catch `saveAttachment()`                                                                                | Failed save leaves image in UI with empty path; user loses attachment on next save                                     |
| 29  | MEDIUM   | code-quality    | `app/page/[id].tsx:43-50`                                          | Duplicate `generateUUID()` — already exported from `useStorage.ts`                                                                                 | Two independent UUID generators; theoretical collision risk                                                            |
| 30  | MEDIUM   | code-quality    | `src/lib/storage/local.web.ts:86-106`                              | IndexedDB transaction errors — no timeout, no partial write cleanup                                                                                | Hung promises if quota exceeded; no recovery path                                                                      |
| 31  | LOW      | code-quality    | `packages/canto-data/src/validation.ts:140-147`                    | GeoLocation validation accepts any number for lat/lon (no range check)                                                                             | Invalid coordinates pass validation; no impact on storage but bad data quality                                         |
| 32  | LOW      | code-quality    | `packages/canto-data/src/validation.ts:183-196`                    | No validation for empty/whitespace-only tags                                                                                                       | Empty tags bloat search index and filter bar                                                                           |
| 33  | LOW      | security        | `src/lib/sync/gdrive/api.ts:91,124`                                | Hard-coded multipart boundary `'---canto-boundary---'`                                                                                             | Metadata containing this string could corrupt multipart encoding                                                       |
| 34  | LOW      | code-quality    | `src/lib/sync/manager.ts:89-97`                                    | Sync error captures only message, discards stack trace                                                                                             | Difficult to debug sync failures in production                                                                         |
| 35  | LOW      | code-quality    | `src/lib/storage/local.ts:302-314`                                 | Soft-deleted pages don't clean up orphaned attachments                                                                                             | Storage grows with unreachable attachment files                                                                        |
| 36  | LOW      | code-quality    | `src/components/page/TagEditor.tsx:24-31`                          | Tags normalized for comparison but stored as-entered                                                                                               | Potential duplicates: "#Python" and "#python" coexist                                                                  |
| 37  | LOW      | test-gap        | multiple                                                           | Missing planned tests: native downloadAttachment, FilterBar debounce, ImageCarousel edit→save cycle, PageListItem dimensions, accessibility labels | 0% coverage on native download path; no regression test for core bug fix                                               |
| 38  | LOW      | test-gap        | `src/components/home/__tests__/InfoBox.test.tsx`                   | act() console warnings from async `loadChangelog`                                                                                                  | Noisy test output; false alarms in CI                                                                                  |
| 39  | LOW      | code-quality    | `src/contexts/GoogleAuthContext.web.tsx:62-80`                     | Stored auth JSON parsed without schema validation                                                                                                  | Corrupted localStorage silently fails auth with no logging                                                             |
| 40  | LOW      | code-quality    | `src/contexts/GoogleAuthContext.web.tsx:187-214`                   | `getAccessToken()` retries silent re-auth without timeout or retry limit                                                                           | Potential app freeze on slow/failing OAuth provider                                                                    |

## Priority Legend

- **CRITICAL**: Can cause data loss or corruption in normal usage. Fix before next release.
- **HIGH**: Can cause data loss in edge cases, or is a security vulnerability. Fix soon.
- **MEDIUM**: Code quality, maintainability, or minor bugs. Schedule for upcoming work.
- **LOW**: Polish, test gaps, minor improvements. Address opportunistically.

## Stats

- **CRITICAL**: 5 issues (all data corruption / race conditions)
- **HIGH**: 9 issues (data corruption, security, bugs)
- **MEDIUM**: 16 issues (code quality, bugs, DRY violations)
- **LOW**: 10 issues (test gaps, polish, minor quality)
- **Total**: 40 issues
