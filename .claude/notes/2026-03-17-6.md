# 2026-03-17 - Google Cloud Project Setup Guide (Requirement 11)

No Expo account required — uses native deep-link redirect via the app's `scheme: "canto"` (already configured in `app.json`).

## 1. Create a Google Cloud Project

1. Go to [Google Cloud Console](https://console.cloud.google.com/)
2. Click **Select a project** → **New Project**
3. Name it (e.g., `canto-journal`) and click **Create**

## 2. Enable the Google Drive API

1. In the project dashboard, go to **APIs & Services** → **Library**
2. Search for **Google Drive API** and click **Enable**

## 3. Configure OAuth Consent Screen

1. Go to **APIs & Services** → **OAuth consent screen**
2. Select **External** user type (unless you have a Google Workspace org)
3. Fill in:
   - **App name**: Canto
   - **User support email**: your email
   - **Developer contact**: your email
4. Click **Save and Continue**
5. On the **Scopes** step, click **Add or Remove Scopes** and add:
   - `https://www.googleapis.com/auth/drive.appdata`
   - `https://www.googleapis.com/auth/drive.file`
6. Save and continue through the remaining steps
7. Under **Test users**, add your Google account email(s)

> **Note**: While in "Testing" mode, only listed test users can authenticate. To allow any Google user, you'll need to publish the app and go through Google's verification process.

## 4. Create OAuth Credentials

You need **three** OAuth client IDs (one per platform). No Expo proxy is used.

### 4a. Web client ID (required for token exchange AND web platform OAuth)

1. Go to **APIs & Services** → **Credentials**
2. Click **Create Credentials** → **OAuth client ID**
3. Select **Web application**
4. Name it (e.g., `Canto Web`)
5. **Authorized JavaScript Origins** (required for CORS on web platform):
   - `http://localhost:8081` (Expo dev server)
   - `http://localhost:19006` (Expo web alt port)
   - Production URL when deployed (e.g., `https://canto.example.com`)
6. **Authorized Redirect URIs** (required for OAuth callback on web platform):
   - `http://localhost:8081`
   - `http://localhost:19006`
   - Production URL when deployed
7. Click **Create** and note the **Client ID**

> **Important for web platform**: Without the JavaScript origins and redirect URIs configured, the browser-based OAuth flow via `expo-auth-session` will fail with a `redirect_uri_mismatch` error. After saving changes, allow ~5 minutes for Google to propagate the configuration.

### 4b. Android client ID

1. Click **Create Credentials** → **OAuth client ID**
2. Select **Android**
3. **Package name**: `com.canto.app`
4. **SHA-1 fingerprint**:
   - For debug: `keytool -keystore ~/.android/debug.keystore -list -v` (password: `android`)
   - For release: use your release keystore
5. Click **Create** and note the **Client ID**

### 4c. iOS client ID

1. Click **Create Credentials** → **OAuth client ID**
2. Select **iOS**
3. **Bundle ID**: `com.canto.app`
4. Click **Create** and note the **Client ID**

## 5. Store Credentials in the Project

A template file already exists at the project root: `google-credentials.ts` (gitignored).

Replace the `REPLACE_ME` placeholders with your actual client IDs:

```typescript
export const GOOGLE_CREDENTIALS = {
  webClientId: '<your-web-client-id>.apps.googleusercontent.com',
  androidClientId: '<your-android-client-id>.apps.googleusercontent.com',
  iosClientId: '<your-ios-client-id>.apps.googleusercontent.com',
};
```

## 6. How Authentication Works

### Native (Android/iOS) — `@react-native-google-signin/google-signin`

The native platforms use the `@react-native-google-signin/google-signin` library which integrates with Google Play Services (Android) and native Sign-In frameworks (iOS). Token refresh is handled automatically by the OS. Session persistence is built-in.

- Configuration: `GoogleSignin.configure({ webClientId, scopes })`
- The **web client ID** is used for server-side token exchange
- File: `src/contexts/GoogleAuthContext.tsx`

### Web — `expo-auth-session/providers/google`

The web platform uses `expo-auth-session` with the Google provider for browser-based OAuth:

```typescript
import * as Google from 'expo-auth-session/providers/google';

const redirectUri = AuthSession.makeRedirectUri(); // returns current page URL on web
```

- Uses OAuth 2.0 authorization code flow with PKCE (secure, gives refresh tokens)
- The **web client ID** is passed as `clientId`
- Redirect URI = current page URL (must be in GCP Authorized Redirect URIs)
- Refresh tokens stored in `localStorage` with configurable retention (1 day / 1 week / 1 month / never expire)
- Silent refresh on page load via `AuthSession.refreshAsync()`
- File: `src/contexts/GoogleAuthContext.web.tsx`

## 7. Scopes Summary

| Scope           | Purpose                                                     |
| --------------- | ----------------------------------------------------------- |
| `drive.appdata` | Read/write to app-specific hidden folder (journal registry) |
| `drive.file`    | Read/write files created by the app (journal data)          |

These are narrow scopes — the app cannot access the user's other Drive files.
