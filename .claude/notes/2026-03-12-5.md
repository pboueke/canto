# Note: Requirement 2 Patch Fixes

**Date:** 2026-03-12

## Fixes Applied

1. **Header overlapping Android notification bar** — Added `useSafeAreaInsets()` to home screen (`paddingTop: insets.top`), journal header, and page header. Content now respects the safe area.

2. **Journal selection flex row** — Replaced `FlatList` with `numColumns` with a `ScrollView` containing a flex-wrap row. All journal cards AND the "New Journal" button now sit together in the same row, wrapping naturally.

3. **About Canto link** — Now calls `Linking.openURL('https://github.com/pboueke/canto')` to open the repo in the browser.

4. **CHANGELOG.md + version popup** — Created `CHANGELOG.md` in repo root and `src/lib/changelog.ts` with the changelog content and `APP_VERSION` constant. Tapping the version in InfoBox opens a modal displaying the changelog.

5. **Journal page header buttons** — Added settings (gear) and data (hard drive) buttons to `JournalHeader`, matching the legacy app's two right-side action buttons. Props `onPressSettings` and `onPressData` are available for future modal wiring.

6. **Back arrows** — Created `BackButton` component (`src/components/common/BackButton.tsx`) with a left arrow that calls `router.back()`. Added to both `JournalHeader` and `PageHeader`.

## Verification

- TypeScript: 0 errors
- ESLint: 0 errors
- Jest: 20/20 tests passing
