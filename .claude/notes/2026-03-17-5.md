# Cleanup Round 2 — Best Practices & README (v0.10.0)

## What was done

### ESLint (Phase 1)

- Added `@typescript-eslint/consistent-type-imports` rule (warn, inline-type-imports style)
- Added `no-console` rule (warn, allows `warn` and `error`)
- Fixed all violations: type imports converted, unused imports removed

### Test Coverage (Phase 2)

- Raised coverage threshold from 40% → 80% statements
- Added 52 new tests across 3 test files:
  - `src/hooks/__tests__/useStorage.test.ts` — 38 tests for all hooks (useJournals, useJournal, usePage, useSavePage, useCreateJournal, useCreatePage, useDeletePage, useDeleteJournal, useSaveJournal, useJournalTags, useAttachment) plus utility functions (getEncryptionService, getLocalStore, tryLoadJournal)
  - `src/hooks/__tests__/useImageQueue.test.ts` — 10 tests for useImageQueue hook and enqueueThumbnail
  - `src/lib/__tests__/changelog.test.ts` — 4 tests for parseVersionFromChangelog
- Final coverage: 95.45% statements, 317 tests total

### Security Review (Phase 3)

- Full OWASP Mobile Top 10 audit — no critical issues found
- All crypto randomness uses `crypto.getRandomValues()` (secure)
- No hardcoded secrets, no eval(), no insecure HTTP
- Console logging reviewed — only `warn`/`error` level, no sensitive data leaked

### Repository Automation (Phase 4)

- Added `test:ci` npm script (`jest --coverage --ci`)
- Pre-commit hook now syncs test count badge in README
- Verified existing hooks work correctly

### Code Duplication (Phase 5)

- Consolidated `generateUUID()` into `src/lib/encryption/utils.ts` (was duplicated in `useStorage.ts` and `backup/import.ts`)
- Consolidated `base64ToUint8()` usage — `JournalKeyContext.tsx` now imports from `encryption/utils.ts` instead of having its own copy
- Removed duplicate `uint8ToBase64()` from `useStorage.ts` — now imports from `encryption/utils.ts`

### README (Phase 6)

- Fixed inaccuracies: i18n (2→8 languages), themes (2→6), crypto provider (noble→expo-crypto), PBKDF2 default (600k→50k)
- Expanded Architecture section into full Security Model section covering: encryption layers, key management, session security, backup encryption, cryptographic dependencies
- Added test count badge (auto-updated by pre-commit hook)
- Updated project structure diagram
- Updated test count (81→317) and coverage threshold (40→80%)

## Files modified

- `eslint.config.mjs` — new rules
- `jest.config.js` — coverage threshold 40→80
- `package.json` — test:ci script
- `README.md` — full rewrite
- `CHANGELOG.md` — v0.10.0 entry
- `.husky/pre-commit` — test badge sync
- `src/hooks/useStorage.ts` — import consolidation
- `src/contexts/JournalKeyContext.tsx` — import consolidation
- `src/lib/backup/import.ts` — import consolidation
- `src/lib/encryption/utils.ts` — added generateUUID
- `src/lib/__tests__/backup.test.ts` — removed unused imports

## Files created

- `src/hooks/__tests__/useStorage.test.ts`
- `src/hooks/__tests__/useImageQueue.test.ts`
- `src/lib/__tests__/changelog.test.ts`
