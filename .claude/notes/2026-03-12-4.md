# Note: Styling & Navigation Implementation (Requirement 2)

**Date:** 2026-03-12

## What was done

### Static Assets

- Copied logo files from reference repo: `canto_bv1.png`, `canto_bv1.svg`, `canto_wv1.svg`, `canto_banner_v1.png`, `logo_t_v1_256.png` to `src/assets/images/`
- Copied Lato and Merriweather font files (Regular, Bold, Light, Italic variants) to `src/assets/fonts/`

### Theme Update

- Updated `src/styles/themes.ts` to match the original app's exact color scheme
- Light theme: teal primary (`rgb(1,85,92)`), gray background (`rgb(190,190,190)`), white foreground, black borders (2px)
- Dark theme: pink primary (`rgb(255,161,186)`), black background, dark gray foreground, gray borders (1px)
- Added new theme properties: `foreground`, `highlight`, `editBar`, `deleteAction`, `headerBackground`, `filterRow`, `borderWidth`, `newJournal`, `popAction`, `location`

### Components Created

**Common** (`src/components/common/`):

- `Logo.tsx` — Theme-aware PNG logo with tint for dark mode
- `FloatingActionButton.tsx` — Circular FAB with configurable position (left/right)
- `Tag.tsx` — Pill-shaped tag component (border-radius 20px)
- `Card.tsx` — Bordered card with theme colors and elevation

**Home** (`src/components/home/`):

- `InfoBox.tsx` — Right-side info panel with theme/language toggles, about link, version
- `JournalCard.tsx` — Journal selector card with emoji icon, name, entry count
- `NewJournalCard.tsx` — "New Journal" card with gold/dark background

**Journal** (`src/components/journal/`):

- `PageListItem.tsx` — Entry card with date, time, tags, preview text, attachment icons
- `JournalHeader.tsx` — Header bar with journal icon and name

**Page** (`src/components/page/`):

- `PageHeader.tsx` — 55px header with date/time display
- `PageContent.tsx` — Text content area with view/edit modes (Merriweather font)
- `TagsRow.tsx` — Centered row of tag pills
- `AttachmentBar.tsx` — Placeholder bar showing image/file/location indicators

### Screens Updated

- `app/index.tsx` — Two-column layout (logo left, info right) + journal card grid
- `app/journal/[id].tsx` — Journal header + page list + floating "new page" button
- `app/page/[id].tsx` — Full page view with header, tags, content, attachments, comments, location, edit/delete FABs

### Layout Updated

- `app/_layout.tsx` — Font loading with `useFonts`, splash screen management, hidden headers for all screens

### Mock Data

- `src/lib/mockData.ts` — 3 journals, 6 pages with markdown content, tags, and attachment flags
- Helper functions: `getJournal()`, `getJournalPages()`, `getPage()`

### Tests

- Updated `__tests__/themes.test.ts` for new theme structure and original colors
- Added `__tests__/mockData.test.ts` with 7 tests
- Converted `jest.config.ts` → `jest.config.js` to avoid ts-node dependency
- All 20 tests passing

### Verification

- TypeScript: 0 errors
- ESLint: 0 errors, 0 warnings
- Jest: 20/20 tests passing
