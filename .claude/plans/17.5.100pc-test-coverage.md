# Plan: 100% Test Coverage (Requirement 17.5)

## Current State

- **Threshold**: 80% statements → target 100%
- **Current coverage**: 81.55% statements, 68.64% branches, 79.26% functions, 82.83% lines
- **Excluded from coverage**: `src/components/**`, `app/**`, `*.d.ts`
- **Failing tests**: 7 tests in 4 suites (component tests — excluded from coverage)

## Strategy

Many 0% files are **type-only** or **re-export barrels** with no executable code. These should be excluded from coverage rather than tested. Files with actual logic need tests added or gaps filled.

### Phase 1: Clean up coverage config (exclude type-only/barrel files)

Add exclusions to `collectCoverageFrom` in `jest.config.js` for files that contain only types, interfaces, or re-exports:

- `!src/data/**` — all 4 files are pure re-exports of `canto-data`
- `!src/lib/encryption/types.ts` — interfaces only
- `!src/lib/storage/types.ts` — interfaces only
- `!src/lib/sync/types.ts` — interfaces only
- `!src/lib/devTools.ts` — dev-only tooling, not testable in Jest context (depends on runtime store/encryption singletons)
- `!**/index.ts` — barrel re-exports (backup/index.ts, storage/index.ts, sync/index.ts, gdrive/index.ts, canto-data/src/index.ts)

Update threshold to `100` for all metrics (statements, branches, functions, lines).

### Phase 2: Fill coverage gaps in `canto-data` package

**`packages/canto-data/src/format.ts`** (91.11% → 100%)

- Add tests for uncovered lines 58, 72, 76, 80 (edge cases in format functions)

**`packages/canto-data/src/validation.ts`** (95.65% → 100%)

- Add test for uncovered line 204

### Phase 3: Fill coverage gaps in `src/lib/`

**`src/lib/changelog.ts`** (33.33% → 100%)

- Test `loadChangelog()` — mock `expo-asset` and `expo-file-system/next`
- `parseVersionFromChangelog()` already has some coverage

**`src/lib/changelog.web.ts`** (0% → 100%)

- Test `loadChangelog()` — mock `expo-asset` and `fetch`
- Test `parseVersionFromChangelog()`

**`src/lib/thumbnail.ts`** (85.71% → 100%)

- Add tests for uncovered branches

**`src/lib/backup/export.ts`** (98.03% → 100%)

- Cover remaining edge case(s)

**`src/lib/backup/export.web.ts`** (95.83% → 100%)

- Cover remaining edge case(s)

**`src/lib/backup/import.ts`** (94.33% → 100%)

- Cover remaining branches

**`src/lib/backup/import.web.ts`** (83.01% → 100%)

- Cover remaining branches/error paths

**`src/lib/encryption/device.ts`** (95.34% → 100%)

- Cover remaining branches

**`src/lib/encryption/device.web.ts`** (95.55% → 100%)

- Cover remaining branches

**`src/lib/encryption/ratelimit.ts`** (95.23% → 100%)

- Cover remaining branches

**`src/lib/encryption/utils.ts`** (93.68% → 100%)

- Cover remaining utility branches

**`src/lib/storage/local.ts`** (94.4% → 100%)

- Cover remaining branches

**`src/lib/storage/local.web.ts`** (82.67% → 100%)

- Cover remaining branches and error paths

**`src/lib/sync/engine.ts`** (96.51% → 100%)

- Cover remaining sync edge cases

**`src/lib/sync/gdrive/api.ts`** (93.18% → 100%)

- Cover remaining API error paths

**`src/lib/sync/gdrive/store.ts`** (81.17% → 100%)

- Cover remaining store operations and error paths

### Phase 4: Fill coverage gaps in `src/hooks/`

**`src/hooks/useSafeBack.ts`** (0% → 100%)

- Test both branches: canGoBack → router.back(), else → router.replace('/')
- Mock expo-router's `useRouter` and `useNavigationContainerRef`

**`src/hooks/useImageQueue.ts`** (90.32% → 100%)

- Cover remaining queue edge cases

**`src/hooks/useStorage.ts`** (98.02% → 100%)

- Cover remaining edge case(s)

### Phase 5: Fill coverage gaps in `src/contexts/`

**`src/contexts/GoogleAuthContext.tsx`** (0% → 100%)

- Test `GoogleAuthProvider` rendering
- Test silent sign-in on mount (success, SIGN_IN_REQUIRED, other error)
- Test `signIn` (success, cancelled, error)
- Test `signOut` (success, error)
- Test `getAccessToken`
- Test `hasValidCredentials` false path
- Mock `@react-native-google-signin/google-signin`

**`src/contexts/GoogleAuthContext.web.tsx`** (57.14% → 100%)

- Extend existing `GoogleAuthContextWeb.test.tsx` to cover remaining branches

**`src/contexts/JournalKeyContext.tsx`** (69.81% → 100%)

- Extend existing `journalKeyContext.test.ts` to cover remaining branches

**`src/contexts/SyncManagerContext.tsx`** (0% → 100%)

- Test `SyncManagerProvider` rendering
- Test `useSyncState` hook (subscribe/getSnapshot/state changes)
- Test `syncJournal`, `scheduleSyncDebounced`, `getSyncState`
- Test disconnect on unmount and on accessToken becoming null
- Mock `GoogleAuthContext`, `SyncManager`, `GDriveRemoteStore`, `getLocalStore`

## Execution Order

1. Phase 1 first — reduces the surface area and sets the target
2. Phases 2-5 iteratively — run `npx jest --coverage` after each file to verify progress
3. After all phases, verify the 100% threshold passes

## Notes

- Component tests (`src/components/**`) and route files (`app/**`) remain excluded — they are UI tests, not unit-testable in the same way
- The 7 currently failing component tests should be fixed separately (not in scope for this requirement, since components are excluded from coverage)
- Each phase will be committed separately with passing tests
