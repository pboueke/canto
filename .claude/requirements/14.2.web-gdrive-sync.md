- Google Sign-In and Google Drive sync must work on the web platform
- The current web stub (`GoogleAuthContext.web.tsx`) provides no-op functions and warns "not available on web"
- The sync engine, manager, and Google Drive API layer all use plain `fetch()` and are already platform-agnostic — only authentication is missing on web
- Implement Google OAuth 2.0 for web using Google Identity Services (GIS) library
- The existing web client ID (`google-credentials.ts`) should be reused
- Token refresh must work so the user doesn't have to re-authenticate frequently
- The sync button in JournalHeader must be shown on web (currently hidden with Platform.OS check)
- AccountButton must work on web (currently calls no-op signIn)
- All existing sync UI (SyncModal, AccountButton, manage journals) should become functional on web
- Tests must cover the web auth flow
