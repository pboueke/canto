- remove all mocked journas and entries
- implement the journal creation mechanism. When pressing the new journal button the user should see a popup asking for a journal name, icon selection and an optional passwoRD
- when the user creates a journal, the journal metadata must be created. The salt must be stored along side so it can be retrieved and used in different devices
- add a simgle mocked entry to every new journal so navigation still works
- during the plan phase, review the reference repository to see if any missing journal cration logic is not described here. Skip remote storage setup for now, but expect this as an optional component in the creation flow
