- changelog and help button look like the same button, they should not share the same underline
- when opening the changelog modal, it should be tabbed. Default view shows changelog, secodary tab shows a manifest with all npm dependencies used in canto and thir licenses
