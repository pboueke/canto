- review the note 2026-03-13-7.md and propose a file backup system for canto
- in the journal page, the save btn should be replaced with an export/backup button in which the user can generate a .zip file containing their entire journal (all metadata, all attachments, pages, etc)
- the user can select if they want to generate the export with encrypted or unencrypted files
- in the new journal creation component, a new option at the end should allow the user to select an exported journal to import it into the app
- backups are per journal
- the app needs to detect name conflicts and let the user rename their journal if thats the case
- canto should allow the user to reimport the same journal multiple times, creating a copy every time
