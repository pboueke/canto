- page previews take too long to load beause of how encrypted images are loaded. Instead, an encrypted thumbnail should be generated whenever an unencrypted image is present. When a page is saved containing at least one unencrypted image, it should be used to generate a small thumbnail for the page preview
- pages date and hour shouldbe editable if user clicks on them on edit mode
- if user is editing and has altered the page content, if they click to exit edit mode or leave the page, a modal should be raised asking if they want to discard the changes
- the android navigation bar on the bottom of the page is o top of the + button on the page view
- journal screen should use scroll pagination, loading the first 15 pages at first and only loading more if user scrolls down
- I need a way to add journals and pages via a dev menu. It could be either via console or a developer menu. I need this to execute performance tests. It should allow me to duplicate a specific journal or page how many times I want, or just add many black journal/page
- app icon on device still uses default expo logo. Actual app logo already exists at src/assets/images. Use them instead
