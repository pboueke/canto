- retrieve te canto log files from the reference repo (../copy/canto) and crate a static dir for suche files
- replicate the original home page style in the new app. Use same color schemes
- add the main components and subcomponents needed for navigation
- where needed, create mocked instances so we can navigate between components (a mock jounal in the landing page and a mocked post in the post page)

Patch:

- Header contents are too high, being overlayed with the android notifications bar. Fix this problem
- update the journal selection so all journals, inclusing the new journal btn, belong to a flex row
- about Canto should redirect to the canto repo
- create a CHANGELOG.md file in the repo, where the latest header should contain the current app version. When clicking on the version in the home page, a popup with the changelog should be seen
- update the journal page header so it includes the same buttons as in the legacy app
- add back arrows so use can return to the previous page
