- the cog on the journal page should open a full height/width configuration component
- the configuration component must have the same options as the reference app, such as:
  - delete journal (asks confirmation with password. If no password, asks user to write 'delete {journal name}')
  - option to change the password, which decrypts and reencrypts all posts and assets encrypted with that password
  - anything else available in the reference app
  - skip remote storageconfigurations for now
- the journal must have the filter bar seen in the reference app. It must include:
  - query filter that filters view to only contain the inputed text (in the unencrypted preview text)
  - attachment filter (filter by entries with at least one attachment type)
  - tag filter
  - date range filter
  - multiple filters can be active at the same time, and a clear filters btn must exist
