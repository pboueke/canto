- create a filesystem library that will be used by canto to store posts on the current device and on a remote store
  - it should use the device encryption layer to automatically encrypt data if possible on the device
  - it should allow for another aplication-layer encryption mechanism, in which the a password and salt can be inputed to encrypt/decrypt a file
- create an empty remote store interface that can be e implemented with different storage providers (ftb, google drive, etc) which can be used to sync files from the device to the remote store
  - ensure that the interface between this module and the filesystem library is already in place

The objective is to provide a data layer for storing and syncing journal metadata, entries and assets between the device and the remote store

Patch:

- update the changelog module so instead of duplicating the content on both the md and ts files, the md file is bundled with the app and then read by the changelog module
