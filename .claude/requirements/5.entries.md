- remove the mocked entry
- update the + button in the journal page so it moves the user to the new entry page
- pages have two modes: edit and preview. In preview mode, markdown and attachments are rendered and in edit mode, raw text can be edited and attachments can be added or removed
- review the reference app for the exact look of the entry page and how the attachments are displayed and added
- entries need to be composed of an editable body, editable list of tags and specific attachment objects:
  - image:
    - add: user clicks the add image option and gets a media selector
    - preview: one ore many, must be displayedin a carrosel, the user must be able to remove added files and change their order in the carrosel
  - encrypted image:
    - add: user clicks the add image option and gets a media selector
    - preview: same as above, but in a different carrosel with a lock icon. Decryption must be async so it may take a while before the images are available
  - file:
    - add: user clicks the add image option and gets a file selector
    - displays as a filename that the user can click and have their os suggest an action for how to open it
  - encrypted file:
    - add: user clicks the add image option and gets a file selector
    - preview: displayed as a filename. If the user clicks it, it gets the password prompt again and if they succeed, the file is decrypted in their donwload folder
  - geo tag:
    - add: user click to add geo tag and immediately lat/lon data is added to the post
    - preview: when the user clicks the lat/lon, gets redirected to default map app on tese coordinates
  - tags:
    - add: user gets a popup suggesting other tags availabe in the journal or to create a new one
    - preview: displayed automatically as a little tag in the bottom of the page
- all of these attachments must have an X symbol in edit mode that allows them to be deleted
- pages are stored as in the disk similar to how journals are, but all of the attachemnts and text is encrypted with the journal password
- journal metadata must include references to the pages so they ca be displayed in the journal page
- page metadata must include tags, the first line and the first unencrypted image so the pages can be previewed in the journal
- plan ahead for portability features that would allow forthe full page to be exported out of canto
