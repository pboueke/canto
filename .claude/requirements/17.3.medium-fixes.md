Fix all 16 MEDIUM priority issues identified in the codebase audit (`.claude/notes/2026-03-20-6.md`, v0.17.0).

These are code quality improvements, DRY violations, minor bugs, and hardening:

15. Cloud import Promise.all for attachments — one failure aborts all but metadata may be partially saved (NewJournalModal.tsx)
16. Inconsistent folder cache key format — collision risk for same-name folders (gdrive/store.ts)
17. ~450 lines duplicated business logic between native and web storage (local.ts + local.web.ts)
18. 95% identical export logic duplicated across platforms (export.ts + export.web.ts)
19. Identical import logic duplicated across platforms (import.ts + import.web.ts)
20. Attachment encryption flag set based on current key, not original intent (import.ts)
21. No validation that access token is non-empty before storing (gdrive/store.ts)
22. Inconsistent JSON.parse error handling — safeJsonParse exists but not used everywhere (multiple)
23. Type assertion `as JournalContent` without runtime validation (import.ts)
24. Unused `t` in handleDownloadImage dependency array (page/[id].tsx)
25. Debounce sync useEffect is one-directional (FilterBar.tsx)
26. Dead fallback branch — searchText is required (useFilter.ts)
27. DRY violation — handleOpenFile duplicates downloadAttachment logic (page/[id].tsx)
28. handleAddImage/handleAddFile don't try-catch saveAttachment (page/[id].tsx)
29. Duplicate generateUUID — already exported from utils (page/[id].tsx)
30. IndexedDB transaction errors — no timeout, no partial write cleanup (local.web.ts)
