Fix all 10 LOW priority issues identified in the codebase audit (`.claude/notes/2026-03-20-6.md`, v0.17.0).

These are polish, test gaps, and minor quality improvements:

31. GeoLocation validation accepts any number for lat/lon — no range check (validation.ts)
32. No validation for empty/whitespace-only tags (validation.ts)
33. Hard-coded multipart boundary in GDrive API — content collision risk (api.ts)
34. Sync error captures only message, discards stack trace (manager.ts)
35. Soft-deleted pages don't clean up orphaned attachments — storage grows (local.ts)
36. Tags normalized for comparison but stored as-entered — potential duplicates (TagEditor.tsx)
37. Missing planned tests: native downloadAttachment, FilterBar debounce, ImageCarousel edit→save, PageListItem dimensions, accessibility labels
38. act() console warnings from async loadChangelog in InfoBox test (InfoBox.test.tsx)
39. Stored auth JSON parsed without schema validation (GoogleAuthContext.web.tsx)
40. getAccessToken retries silent re-auth without timeout or retry limit (GoogleAuthContext.web.tsx)
