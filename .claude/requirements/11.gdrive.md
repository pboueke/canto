- implement an automatic backup mechanism using the sync interface targetting google drive
  - the reference app has that functionality, but it it not fully tested and sync issues existed. The reference app has not been updated in 5 years, so it is probably too outdated anyway
- the google drive mechanism must work on a per-journal basis: by the side of the export button, create a sync button. When clicked, a modal must open showing which sync method is enabled for that journal, if any
- use oauth mechanism to login to google account in the home page. An accounts button needs to be added to the home page footer
  - if logged in, show small user avatar. If user clicks in it, give option to logout
- if user is logged in, they need to open their journal and click the sync button to add google drive as sync mechanism to that journal
- user must be able to create a journal in one device, sync it to drive, login in another device, view which journals are available there and sync them to their device
- continue using sync protocol and expect diferent providers to be used in the future
