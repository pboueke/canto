- Add schema migration and settings validation to the import pipeline
- When importing a backup with an older schemaVersion, run migrateIfNeeded() from canto-data before saving
- Validate settings with validateJournalSettings() after reading settings.json
- Add tests for importing legacy (pre-0.17.0) backups and future version rejection
- Apply to both import.ts (native) and import.web.ts (web)
