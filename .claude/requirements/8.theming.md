- update the home screen theme change button to open a modal where the user can select a theme
- include theme previews inside the theme modeal
- keep the 2 current themes and create 4 more based on popular open source IDE themes
- update the home screen language button to open a modal where the user can select the language
- add these languages to the language selection and dictionary: spanish, german, french, russian, chinese, italian
- fix the usability issue in which if I input a bad password, I am redirected to the journal not found page instead of getting an error message
- the journa previews do not look great, update them so:
  - first row contains small badges
  - at the center, the icon is displayed 50% larger tha it currently is
  - below it comes the journal name
  - all of these items must be centraized
- journals should allow a theme override. When created, use the currently global theme, but the settings modal should include the theme selector option that will override the theme only for that journal
