- the existing export/import system is very fragile, new issues are found on every QA run. We need a new, comprhensive, end-to-end test suite to cover the full flow from generating backus to importing them. Add a new test file for this purpose. It needs to:
  - cover all conditional flows for journals with and w/o passwords
  - include all attachment types
  - actually execute the code for each edge case
  - validate the equivalence of the data after the import to the data from the backuped journal
- the manual QA process for journal backups should consist mostly of interface checks. All data handling should be convered by the new test suite
