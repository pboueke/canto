- new filter in journal page named "anniversary", which shows pages that were created in the same day/month on previous years
- new calendar button in the journal header (left of remote sync) open new per-journal calendar page
  - calendar page contains:
    - calendar header '{Journal title} Calendar'
    - Anniversary row telling user how many pages are celebrating creation anniversaries
    - scroll of month calendars with highlighed days whenever there is a page on that day
    - if the user clicks the anniversary row, gets redirected to journal page with anniversary filter enabled
    - if user clicks on a calendar preview, gets redirected to journal age with date filter matching clicked calendar
- calendar scroll state must be preserved between page switching
