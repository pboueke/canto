- style picker must include font customization:
  - font size (with 4 presets: small, default (current), large, extra large)
  - font type: keep curent as default, add open dyslexic and an open serif one
- when decrypting a diary, modal should say decrypting instead of loading
- when selecting a gdrive journal to import, there should be a loading modal
- during gdrive import, pages may timeout and fail the full import. A retry mechanism must be added so each page is retried 4 times (5 attempts per page)
- as usertypes text in the journal page, the text area grows untill it is covered by the keyboaard. User must always be able to see the last line of the journal text when editing with keyboard open
